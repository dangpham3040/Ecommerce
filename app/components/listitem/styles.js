import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    margin_layout: {
        marginLeft: 25,
        marginRight: 25,
    },
    titleItem: {
        marginTop: 50,
        fontWeight: 'bold',
        fontSize: 30,
        marginTop: 20,
        marginBottom: 10,
        color: "#2A2D3F",

    },
})